---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  # Load-bearing beyond the usual Helm-release-name sense: alloy-syslog-service
  # selects these pods by `app.kubernetes.io/instance`, which is this name.
  # Renaming it silently detaches the syslog LoadBalancer from its only
  # listener, and UDP gives no error when that happens.
  name: alloy
  namespace: monitoring
spec:
  interval: 30m
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  # Report drift, never correct it — see the reasoning in
  # base/metrics-collection/kube-state-metrics.yaml.
  driftDetection:
    mode: warn
  chart:
    spec:
      chart: alloy
      version: "~1.x"
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: monitoring
      interval: 12h
  values:
    # NOTE ON NESTING: mounts, resources and volumes must sit under `alloy.*`
    # and `controller.volumes.extra`. They previously sat at the root of
    # `values:`, where the chart never reads them — Helm ignores unknown keys
    # silently and this chart ships no values.schema.json, so the file looked
    # correct and rendered a container with no limits and no host mounts. The
    # journal pipeline below was reading a /var/log/journal that did not exist
    # inside the container. See #213.
    # seccomp was the other thing missing here. RuntimeDefault is the profile
    # every other workload in this namespace now runs under, and it constrains
    # the syscall surface without touching the uid this collector needs.
    global:
      podSecurityContext:
        seccompProfile:
          type: RuntimeDefault

    # The chart's configReloader default sets requests but no limits; the
    # ResourceQuota on this namespace requires both.
    configReloader:
      resources:
        requests:
          cpu: 10m
          memory: 50Mi
        limits:
          cpu: 50m
          memory: 100Mi

    controller:
      type: daemonset

      volumes:
        extra:
          # /run/log/journal, not /var/log/journal. journald here runs in
          # volatile mode: /var/log/journal exists but is empty, and the actual
          # journal lives on tmpfs under /run. Raspberry Pi OS Lite is built for
          # SD-card longevity, so this is the image doing the right thing.
          #
          # Reading the volatile journal is also the correct choice rather than
          # a workaround. Enabling persistent journald would put a continuous
          # write stream on the boot medium — the exact wear pattern that keeps
          # every non-Akron site from running a Prometheus TSDB. Alloy ships to
          # Loki within seconds, so Loki is the durable copy and the local
          # journal only has to survive long enough to be read. The cost is the
          # few seconds of entries not yet shipped when a node reboots.
          - name: journal
            hostPath:
              path: /run/log/journal
              type: Directory
          - name: machine-id
            hostPath:
              path: /etc/machine-id
              type: File

    alloy:
      # This one keeps root and a writable root filesystem — it reads
      # /var/log/pods and the volatile journal through hostPath mounts, which is
      # the entire reason it exists, and those findings are accepted in
      # docs/trivy-accepted-findings.txt. Capabilities are a different question:
      # reading root-owned 0644 log files needs none of them, so dropping the
      # set costs nothing and removes the privilege-escalation path that makes a
      # root container worth worrying about.
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL

      mounts:
        varlog: true
        extra:
          - name: journal
            mountPath: /run/log/journal
            readOnly: true
          - name: machine-id
            mountPath: /etc/machine-id
            readOnly: true

      # Observed at ~95MB heap / 155Mi working set while running unconstrained,
      # so 128Mi would have started OOMKilling this the moment a limit actually
      # applied. GOMEMLIMIT sits below the ceiling so the Go runtime collects
      # harder as it approaches rather than being SIGKILLed.
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          cpu: 200m
          memory: 256Mi

      extraEnv:
        - name: GOMEMLIMIT
          value: 230MiB

      extraArgs:
        - --disable-reporting
        - --stability.level=public-preview
      # Config lives beside this file as alloy.alloy — see the note in
      # base/metrics-collection/alloy-metrics.yaml.
      configMap:
        create: false
        name: alloy-logs-config
        key: config.alloy
