# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Claude
.claude/worktrees/**
# trivy-operator audit dumps (docs/runbooks/trivy-operator-audit.md). A snapshot
# of one cluster at one moment; a committed one would later read as current.
*-reports.json
