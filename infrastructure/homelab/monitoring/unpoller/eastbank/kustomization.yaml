---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  - secret.sops.yaml
nameSuffix: -eastbank
patches:
  - target:
      kind: HelmRelease
      name: unpoller
    patch: |
      apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      metadata:
        name: unpoller
      spec:
        valuesFrom:
          - kind: Secret
            name: unpoller-config-eastbank
            valuesKey: upConfig
            targetPath: upConfig
        values:
          podMonitor:
            relabelings:
              - replacement: unpoller-eastbank
                targetLabel: instance
