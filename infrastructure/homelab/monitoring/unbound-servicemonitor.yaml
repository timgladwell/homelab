---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: unbound
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
      - dns
  selector:
    matchLabels:
      app: unbound
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
