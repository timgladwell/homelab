---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pihole
  namespace: dns
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`pihole.${DOMAIN}`)
      kind: Rule
      services:
        - name: pihole-web
          port: 80
