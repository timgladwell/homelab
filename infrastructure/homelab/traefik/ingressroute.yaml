# Traefik dashboard IngressRoute
#
# Routes:
# - http://traefik.PLACEHOLDER_DOMAIN/dashboard/ -> dashboard UI
# - http://traefik.PLACEHOLDER_DOMAIN/api/*      -> dashboard API
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`traefik.PLACEHOLDER_DOMAIN`)
      kind: Rule
      services:
        - name: api@internal
          kind: TraefikService
