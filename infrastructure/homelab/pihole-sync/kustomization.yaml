---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dns
resources:
  - sync-job.yaml
configMapGenerator:
  - name: pihole-sync
    files:
      - pihole-config.yaml
      - sync.py
