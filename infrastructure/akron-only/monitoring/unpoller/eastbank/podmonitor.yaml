---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: unpoller
  namespace: monitoring
spec:
  podMetricsEndpoints:
    - port: tcp
      interval: 30s
      relabelings:
        - replacement: unpoller-eastbank
          targetLabel: instance
        - replacement: eastbank
          targetLabel: site
  selector:
    matchLabels:
      app.kubernetes.io/instance: unpoller-eastbank
      app.kubernetes.io/name: unpoller
